imports:
    - { resource: config.yml }
    
parameters:
    logbook.rest.uri: "http://rest.logbook.me/app_dev.php/"
    logbook.rest.oauth.id: 1_ya1z97izbqooc80c08o0cgcogwcw0g4s4ksgsk84gkwkcco0s # estos parametros dependeran de lo que generes en tu servidor en local
    logbook.rest.oauth.secret: 2vdvviz5j6040cs4g0ow04k8gokwck08kccoscwg0c8wswk04k

framework:
    router:
        resource: "%kernel.root_dir%/config/routing_dev.yml"
        strict_requirements: true
    profiler: { only_exceptions: false }

web_profiler:
    toolbar: true
    intercept_redirects: false

monolog:
    handlers:
        main:
            type:  stream
            path:  %kernel.logs_dir%/%kernel.environment%.log
            level: debug
        firephp:
            type:  firephp
            level: info
        chromephp:
            type:  chromephp
            level: info

assetic:
    use_controller: true
